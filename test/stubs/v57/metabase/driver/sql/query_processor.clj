(ns metabase.driver.sql.query-processor)

(load "/stubs/qp_common")

;;; Shape: Metabase 0.57 - 0.58
;;;   transform-literal-like-pattern-honeysql  ABSENT (added in 0.59)
;;;
;;; Identical to the v50 query-processor shape; the v50/v57 split exists for
;;; `describe-database*` in metabase.driver, not for anything here.

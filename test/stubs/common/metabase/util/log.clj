(ns metabase.util.log
  "Stub of Metabase's logging namespace. Present in every Metabase version the driver targets;
   stubbed here as plain no-op fns so the driver namespace compiles without a Metabase on the
   classpath.")

(defn debugf [& _])
(defn infof  [& _])
(defn warnf  [& _])
(defn errorf [& _])
